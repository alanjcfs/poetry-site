= form_for(@poem) do |f|
  - if @poem.errors.any?
    #error_explanation
      h2 = pluralize(@poem.errors.count, "error") 
        | prohibited this poem from being saved:

      ul
        - @poem.errors.full_messages.each do |msg|
          li = msg

  .field
    = f.label :johnson
    br
    = f.number_field :johnson
  .field
    = f.label :title
    br
    = f.text_field :title
  .field
    = f.label :text
    br
    = f.text_area :text
  .actions
    = f.submit
